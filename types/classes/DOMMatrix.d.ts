
export declare class DOMMatrix extends (window.DOMMatrix || window.WebKitCSSMatrix || {}) {
   a: number
   b: number
   c: number
   d: number
   e: number
   f: number

   constructor(css: string)
}